<section class="cmp-content-page section-spacing">
  <div class="page-width page-width--narrow">
    {% render 'breadcrumbs' %}
    <header><p class="eyebrow">CuidoMisPies</p><h1>{{ page.title }}</h1></header>
    <div class="rte cmp-content-page__body">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Página principal", "settings": [] }
{% endschema %}
